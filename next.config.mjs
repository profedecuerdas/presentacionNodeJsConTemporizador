/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuración opcional si decides desplegar en sitios como GitHub Pages
  // images: { unoptimized: true }
};

export default nextConfig;
